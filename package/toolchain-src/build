mkdir -p "$out"
ln -s "$binutils_src"/* "$out"
ln -s "$gcc_src"/* "$out"
ln -s "$glibc_src"/* "$out"
ln -s "$linux_src"/* "$out"
ln -s "$ncurses_src"/* "$out"
while read -r url; do
  source=$(basename "$url")
  source=$(readlink "$source")
  ln -s "$source" "$out"
done <"$here"/sources
