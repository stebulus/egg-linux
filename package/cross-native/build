awk -v here="$here" \
  -v out="$out" \
  -v src="$crosstool_ng/src" \
  '{
    gsub("@HERE@", here)
    gsub("@OUT@", out)
    gsub("@SRC@", src)
    print
  }' \
  "$here"/config >.config
unset CPATH
unset LD_LIBRARY_PATH
unset LIBRARY_PATH
ct-ng oldconfig
ct-ng build
chmod u+w "$out"
