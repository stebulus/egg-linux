awk -v here="$here" \
  -v out="$out" \
  '{
    gsub("@HERE@", here)
    gsub("@OUT@", out)
    print
  }' \
  "$here"/config >.config
unset CPATH
unset LD_LIBRARY_PATH
unset LIBRARY_PATH
ct-ng oldconfig
ct-ng build
chmod u+w "$out"
