tar xf "$glibc_src"/glibc-2.25.tar.xz
mkdir build
cd build
../glibc-2.25/configure --prefix=/usr \
  --build=x86_64-unknown-linux-gnu \
  --host=x86_64-egg0-linux-gnu
make
make DESTDIR="$out" install
for x in ldd sotruss tzselect xtrace; do
  sed -i -e '1s,.*,#!/bin/bash,' "$out/usr/bin/$x"
done
