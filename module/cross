here=$(dirname "${BASH_SOURCE[0]}")
awk -v here="$here" \
  -v out="$out" \
  '{
    gsub("@HERE@", here)
    gsub("@OUT@", out)
    print
  }' \
  "$here"/cross.config >.config
ct-ng oldconfig
ct-ng build
